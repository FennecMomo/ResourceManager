# 更新日志

项目版本号采用 `系统.模块.修改` 格式，例如 `0.1.1`。Git 提交信息须带上对应的版本号。未发布的变更记录在本文件顶部。

## [未发布]

### 新增

- 客户端 `0.3.7` 新增局域网一对一聊天：文字和资源卡片直连送达，离线消息在发送方本机排队，重新上线后自动补发；聊天历史、未读和静音期限保存在本机。
- 聊天采用 `chat-v1` 能力协商和落库确认，消息按发送设备与固定消息 ID 去重；7 天未送达停止自动重试，失败消息可用原 ID 手动重试。聊天通知在窗口右下角 5 秒自动关闭。
- “我的发布”的“发送给”对支持聊天的设备改为单张持久资源卡片，对旧版设备继续使用一次性提醒；移除设备时可选择保留或删除本机聊天记录。
- 客户端 `0.3.6` 新增独立 Git 协作分页：可检测或一键安装 Git，从本地仓库创建项目，加入附近项目，在协作图中查看每台设备的提交线，并推送自己的已提交内容。
- 协作记录由各成员本机签名并追加，已连接设备互相补齐记录；原始发布者离线时，其他成员仍可继续协作，之后再收敛。
- Git bundle 按 SHA-256 校验并支持断点续传；同步所选提交时仅自动快进，分叉内容先进入收件箱引用等待用户处理。
- 客户端新增“反馈”工作区：标题栏选择局域网服务端或 GitHub 账号授权目标，反馈者填写问题/建议/体验/其他分类、标题和正文，并自动附带 ResourceManager 昵称、客户端版本和 Windows 环境。
- 新增完整本地反馈历史、手动刷新、打开远端、用户软关闭和受限删除；未完成且未由用户关闭的反馈历史不能删除。
- 新增独立版本 `0.0.1` 的 `ResourceManager.Server`：提供自动发现、免配对提交、附件接收、本机管理页、五态本地 Issue 处理和单 GitHub 仓库聚合。
- GitHub 提交使用用户自己的 GitHub App Device Flow 授权，不在客户端或服务端内置共享 PAT、Client Secret 或 App 私钥。

### 安全

- 普通文件夹分享不暴露 `.git` 元数据；Git 协作不发送远端 URL 或本机 Git 配置，暂拒绝子模块、Git LFS、稀疏检出和浅克隆仓库的一键发布。
- 局域网用户无需授权码即可提交；客户端使用随机标识隔离自己的状态查询和关闭操作，提交支持幂等编号、每客户端一分钟一次和全局每小时一百次的服务端限流；附件限制为安全白名单、最多 10 个和总计 50 MB。
- 服务端管理页只监听回环地址；首版局域网 API 按需求使用 HTTP，明确禁止公网映射。

### 修复

- 修正 Git 协作分页与左侧导航错位；设备页手动刷新现在会等待正在进行的状态检查完成，再更新设备状态。
- Git 协作支持把选中分支历史中的 Git LFS 文件与 Git bundle 一起打包，接收端会按对象哈希核对并导入；只有 `.gitattributes` 规则、尚无 LFS 文件的仓库也可以正常创建。
- 修复提交反馈后首次渲染历史记录时，只读展示字段被 WPF 按双向模式绑定并导致客户端崩溃的问题；中断在“提交中”或误记为失败的局域网记录也可通过刷新找回服务端 Issue。
- 提醒接收端会原子登记正在核对的发送请求，阻止并发请求穿过去重和频率限制。
- 提醒协议会把空值备注作为无效请求返回 400，不再触发服务器内部错误。
- 节点只在实际启用提醒端点时声明提醒能力，避免能力列表与可用接口不一致。

### 改进

- 左侧导航底部新增常驻更新提示；客户端每次启动固定检查一次新版本，发现新版时通过系统通知提醒并将入口切换为“可更新”，只有用户点击后才下载和安装。
- 客户端反馈历史使用分类与状态徽标区分已提交、已接受、已拒绝、已完成等阶段；服务端管理页为分类、状态和四个处理动作提供独立语义色，并增加状态筛选。
- 服务端拒绝本地反馈时强制填写并持久化拒绝原因；默认“处理中”列表会归档隐藏已拒绝和已完成条目，仍可通过状态筛选查看历史。
- 反馈目标改为刷新后自动发现单台服务端并置于下拉菜单首项；GitHub 授权栏只在选择 GitHub 目标时显示，下拉菜单与滚轮滚动统一为应用视觉和像素级缓动。
- “发送给”弹窗默认不勾选设备，离线或旧版本设备不可选择，避免误发给全部在线设备。
- 收到的提醒改为在屏幕工作区右下角显示，多个提醒向上堆叠，不主动抢占焦点，并在 5 秒后自动关闭。

## [0.3.4] - 2026-09-23

### 改进

- 设备页移除“直接连接”和“路由器入口”分页：查找局域网设备合并到“已连接的设备”的“刷新”按钮，路由器入口配置移到设置页的“当前路由器”栏。
- 设备备注改为选中设备后点击“备注”按钮在独立窗口编辑；有备注时设备卡片标题显示备注，原昵称显示在副文。
- 资源列表每行新增“发送给”按钮，可在弹窗中勾选多台设备发送提醒；保存资源备注后输入框自动清空。

## [0.3.3] - 2026-09-23

### 新增

- 设备页新增本机备注：以固定设备 ID 关联，设备改昵称、IP 或端口后备注仍然保留；备注只保存在本机，不发送给对方，也不改变对方昵称。
- “我的发布”新增资源备注：备注随资源目录返回给已连接设备，并显示在设备资源列表、收藏详情、提醒弹窗和下载任务中；修改备注不改变资源 ID、发布时间和已有收藏。
- 新增定向资源提醒：发布方可以选择一个可用资源，向在线设备发送提醒；接收端弹出置顶提醒窗口，显示发送设备、资源名称、类型和备注，并可直接选择保存位置创建普通下载任务。

### 改进

- 提醒发送前接收端会重新核对发布方资源目录；未登记来源、已撤销资源、重复提醒和频繁发送都会被明确拒绝。
- 接收方可以在提醒弹窗上对单台设备临时静音 1 小时。

### 限制

- 提醒只通过弹窗即时送达，不在本机保留提醒记录，关闭后需要由发布方重新发送；静音为内存状态，软件重启后失效。
- 跨 NAT 场景下接收端可能无法回连发布方核对目录，此时提醒会明确失败；局域网直连不受影响。
- 0.3.2 及更早版本仍可连接、浏览和下载资源，但不会显示备注，也不参与提醒协议。

## [0.3.2] - 2026-09-23

### 新增

- 更新检查新增局域网本地源：已连接设备发布的 `ResourceManager.exe` 会公开内嵌版本、大小与 SHA-256，可直接作为新版本下载来源；与 GitHub 同版本时要求哈希与官方包一致。

### 修复

- 更新包写入完成后先关闭独占文件流再检查 PE 文件头，避免校验阶段误报文件被占用。
- 安装助手会等待原进程 PID 退出并确认单实例互斥锁已经释放后再替换和重启，同时兼容旧版的三参数安装调用，消除新版提前启动的竞态。

### 改进

- 设备页顶部的“直接连接”和“路由器入口”改为分页标签，减少窄窗口下的拥挤和按钮挤压。
- 更新下载与校验路径新增回归测试，覆盖缓存复用、大小不符和非程序内容。

### 限制

- 0.3.1 及更早版本在下载校验阶段受文件句柄未释放缺陷影响，无法在软件内完成 GitHub 自动更新，需要手动下载本版本替换 EXE；0.3.2 起下载校验已修复。

## [0.3.1] - 2026-09-22

### 新增

- 设备页新增手动路由器入口，可按缓存优先检查指定 WAN IPv4 的 `48001–48099`，并在需要时取消扫描。
- 新增路由器扩展发现协议：引导节点通过现有 UDP 广播获取内部设备名单，目标设备自行申请 UPnP 映射，外部设备验证后直接连接目标端口。
- 设置页新增当前路由器卡片，显示默认路由使用的网卡、路由器本地地址、外部入口 IP、UPnP 状态和本机长期映射，并可复制外部入口或显式删除映射。
- 新增标准 UPnP IGD 客户端，兼容 `WANIPConnection` 与 `WANPPPConnection`，支持映射查询、创建、验证、删除、有限租期续租及每五分钟自动恢复。

### 改进

- 将设备资料和连接端点分开保存；同一个 WAN IP 现在可以通过不同端口连接多台设备，并可在局域网直连与路由器端点之间切换。
- 路由器连接保留实际拨号端口，不再被对方 hello 中的内网监听端口覆盖；缓存端口指向不同设备 ID 时保留原设备并标记端点已变更。
- 数据库升级会保留已有设备、发布资源、收藏和下载任务，并把旧地址迁移为普通直连端点。
- 0.3.0 节点继续支持浏览和下载；只有声明 `router-discovery-v1` 能力的节点会承担扩展发现。

### 限制

- 只支持 IPv4 与 TCP，不包含 IPv6、NAT 打洞或服务器中继。
- 每台路由器仍需至少一条人工创建且在线的首个入口映射，软件不会在完全不可达时自动创建第一条规则。

## [0.3.0] - 2026-09-22

### 新增

- 设备页新增局域网广播查找，可发现同一局域网内正在运行的软件，并通过原有身份握手自动加入设备列表。
- 下载页新增“移除任务”：进行中的任务会先暂停并清理临时内容，已完成任务只移除记录并保留本地文件。

### 改进

- 局域网发现使用独立的 UDP `37643` 端口，只广播设备 ID、昵称和 HTTP 监听端口；资源浏览与下载仍使用原有点对点 HTTP 连接。

## [0.2.3] - 2026-09-22

### 修复

- 修正下载进度条对只读百分比属性使用默认双向绑定的问题，避免下载列表首次渲染时界面线程异常退出。

## [0.2.2] - 2026-09-22

### 修复

- 将资源传输明确移到后台任务，并禁止核心下载流程捕获界面线程上下文，避免高速下载期间界面阻塞。
- 下载进度改为安全的增量更新，避免进度回调异常终止界面线程。
- 退出软件时先取消并收拢正在进行的下载；下载异常会写入本机日志，便于定位现场问题。

## [0.2.1] - 2026-09-21

### 修复

- 增加应用单实例检查，防止重复启动监听服务和本地数据库。
- 再次启动程序时自动显示已有窗口；开机启动遇到已有实例时静默退出。

## [0.2.0] - 2026-09-21

### 新增

- 设置页新增当前用户的 Windows 开机启动选项；登录后可直接在系统托盘运行并开始共享。
- 新增手动检查更新和可选的启动后自动检查；更新包来自 GitHub Release，并在安装前校验大小和 SHA-256。
- 新增标签触发的正式发布流程，自动测试、构建 Windows x64 单文件程序并创建 GitHub Release。

## [0.1.2] - 2026-09-21

### 改进

- 将五个工作区改为侧边导航，使用卡片列表呈现设备、发布资源、收藏和下载任务，并为无内容、离线和不可用状态提供清晰提示。
- 优化主要操作的按钮层级、下载进度和设置布局；发布资源时直接选择“引用原位置”或“保存一份副本”。
- 为侧边品牌标识添加清晰的 PNG 图标，改善高缩放屏幕上的显示。
- 修正桌面快捷方式脚本在 Windows PowerShell 5.1 中读取中文名称的编码。

## [0.1.1] - 2026-09-21

### 改进

- 新增专用的共享文件夹与连接节点图标，并将多尺寸 ICO 嵌入 EXE、窗口和系统托盘。
- 新增当前用户桌面快捷方式安装脚本。

## [0.1.0] - 2026-09-20

### 新增

- 首个 Windows 桌面版本：每台电脑可通过 IP 和端口互连，直接浏览与下载对方发布的文件和文件夹。
- 本地昵称、头像与设备记录；发布时可逐项选择引用原位置或保存管理副本。
- 资源收藏、离线和已撤销状态、保留目录结构的下载与断点续传。
- 默认缩到系统托盘，以及可配置的监听端口和关闭行为。
- SQLite 本地存储、双节点集成测试和 Windows x64 自包含构建流程。
