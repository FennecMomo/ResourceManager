# 更新日志

项目版本号采用 `系统.模块.修改` 格式，例如 `0.1.1`。Git 提交信息须带上对应的版本号。未发布的变更记录在本文件顶部。

## [0.3.1] - 2026-09-22

### 新增

- 设备页新增手动路由器入口，可按缓存优先检查指定 WAN IPv4 的 `48001–48099`，并在需要时取消扫描。
- 新增路由器扩展发现协议：引导节点通过现有 UDP 广播获取内部设备名单，目标设备自行申请 UPnP 映射，外部设备验证后直接连接目标端口。
- 设置页新增当前路由器卡片，显示默认路由使用的网卡、路由器本地地址、外部入口 IP、UPnP 状态和本机长期映射，并可复制外部入口或显式删除映射。
- 新增标准 UPnP IGD 客户端，兼容 `WANIPConnection` 与 `WANPPPConnection`，支持映射查询、创建、验证、删除、有限租期续租及每五分钟自动恢复。

### 改进

- 将设备资料和连接端点分开保存；同一个 WAN IP 现在可以通过不同端口连接多台设备，并可在局域网直连与路由器端点之间切换。
- 路由器连接保留实际拨号端口，不再被对方 hello 中的内网监听端口覆盖；缓存端口指向不同设备 ID 时保留原设备并标记端点已变更。
- 数据库升级会保留已有设备、发布资源、收藏和下载任务，并把旧地址迁移为普通直连端点。
- 0.3.0 节点继续支持浏览和下载；只有声明 `router-discovery-v1` 能力的节点会承担扩展发现。

### 限制

- 只支持 IPv4 与 TCP，不包含 IPv6、NAT 打洞或服务器中继。
- 每台路由器仍需至少一条人工创建且在线的首个入口映射，软件不会在完全不可达时自动创建第一条规则。

## [0.3.0] - 2026-09-22

### 新增

- 设备页新增局域网广播查找，可发现同一局域网内正在运行的软件，并通过原有身份握手自动加入设备列表。
- 下载页新增“移除任务”：进行中的任务会先暂停并清理临时内容，已完成任务只移除记录并保留本地文件。

### 改进

- 局域网发现使用独立的 UDP `37643` 端口，只广播设备 ID、昵称和 HTTP 监听端口；资源浏览与下载仍使用原有点对点 HTTP 连接。

## [0.2.3] - 2026-09-22

### 修复

- 修正下载进度条对只读百分比属性使用默认双向绑定的问题，避免下载列表首次渲染时界面线程异常退出。

## [0.2.2] - 2026-09-22

### 修复

- 将资源传输明确移到后台任务，并禁止核心下载流程捕获界面线程上下文，避免高速下载期间界面阻塞。
- 下载进度改为安全的增量更新，避免进度回调异常终止界面线程。
- 退出软件时先取消并收拢正在进行的下载；下载异常会写入本机日志，便于定位现场问题。

## [0.2.1] - 2026-09-21

### 修复

- 增加应用单实例检查，防止重复启动监听服务和本地数据库。
- 再次启动程序时自动显示已有窗口；开机启动遇到已有实例时静默退出。

## [0.2.0] - 2026-09-21

### 新增

- 设置页新增当前用户的 Windows 开机启动选项；登录后可直接在系统托盘运行并开始共享。
- 新增手动检查更新和可选的启动后自动检查；更新包来自 GitHub Release，并在安装前校验大小和 SHA-256。
- 新增标签触发的正式发布流程，自动测试、构建 Windows x64 单文件程序并创建 GitHub Release。

## [0.1.2] - 2026-09-21

### 改进

- 将五个工作区改为侧边导航，使用卡片列表呈现设备、发布资源、收藏和下载任务，并为无内容、离线和不可用状态提供清晰提示。
- 优化主要操作的按钮层级、下载进度和设置布局；发布资源时直接选择“引用原位置”或“保存一份副本”。
- 为侧边品牌标识添加清晰的 PNG 图标，改善高缩放屏幕上的显示。
- 修正桌面快捷方式脚本在 Windows PowerShell 5.1 中读取中文名称的编码。

## [0.1.1] - 2026-09-21

### 改进

- 新增专用的共享文件夹与连接节点图标，并将多尺寸 ICO 嵌入 EXE、窗口和系统托盘。
- 新增当前用户桌面快捷方式安装脚本。

## [0.1.0] - 2026-09-20

### 新增

- 首个 Windows 桌面版本：每台电脑可通过 IP 和端口互连，直接浏览与下载对方发布的文件和文件夹。
- 本地昵称、头像与设备记录；发布时可逐项选择引用原位置或保存管理副本。
- 资源收藏、离线和已撤销状态、保留目录结构的下载与断点续传。
- 默认缩到系统托盘，以及可配置的监听端口和关闭行为。
- SQLite 本地存储、双节点集成测试和 Windows x64 自包含构建流程。
