<Window x:Class="ResourceManager.App.PublishModeDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="发布资源" Width="500" Height="386" MinWidth="460" MinHeight="360"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize" ShowInTaskbar="False"
        Background="#F6F8FB" FontFamily="Segoe UI">
    <Window.Resources>
        <Style x:Key="Choice" TargetType="RadioButton">
            <Setter Property="Margin" Value="0,0,0,10"/>
            <Setter Property="Padding" Value="16"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#E1E8F1"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border x:Name="ChoiceBorder" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="11" Padding="{TemplateBinding Padding}">
                            <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="22"/></Grid.ColumnDefinitions>
                                <ContentPresenter VerticalAlignment="Center"/>
                                <Border x:Name="Indicator" Grid.Column="1" Width="18" Height="18" CornerRadius="9" BorderThickness="2" BorderBrush="#BBC7D7" VerticalAlignment="Center"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="ChoiceBorder" Property="Background" Value="#F7FAFF"/></Trigger>
                            <Trigger Property="IsChecked" Value="True"><Setter TargetName="ChoiceBorder" Property="Background" Value="#EFF5FF"/><Setter TargetName="ChoiceBorder" Property="BorderBrush" Value="#78A7F8"/><Setter TargetName="Indicator" Property="Background" Value="#2867D9"/><Setter TargetName="Indicator" Property="BorderBrush" Value="#2867D9"/></Trigger>
                            <Trigger Property="IsKeyboardFocused" Value="True"><Setter TargetName="ChoiceBorder" Property="BorderBrush" Value="#2867D9"/><Setter TargetName="ChoiceBorder" Property="BorderThickness" Value="2"/></Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="DialogButton" TargetType="Button">
            <Setter Property="Height" Value="38"/><Setter Property="Padding" Value="18,0"/>
            <Setter Property="Background" Value="#2867D9"/><Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/><Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template"><Setter.Value><ControlTemplate TargetType="Button"><Border Background="{TemplateBinding Background}" CornerRadius="9" Padding="{TemplateBinding Padding}"><ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/></Border></ControlTemplate></Setter.Value></Setter>
        </Style>
    </Window.Resources>
    <Grid Margin="26,24">
        <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
        <TextBlock Text="选择分享方式" FontSize="20" FontWeight="SemiBold" Foreground="#1B2B43"/>
        <TextBlock x:Name="ResourceNameText" Grid.Row="1" FontSize="12" Foreground="#708099" TextTrimming="CharacterEllipsis" Margin="0,5,0,20"/>
        <StackPanel Grid.Row="2">
            <RadioButton x:Name="ReferenceOption" GroupName="PublishMode" IsChecked="True" Style="{StaticResource Choice}">
                <StackPanel><TextBlock Text="引用原位置" FontSize="14" FontWeight="SemiBold" Foreground="#1B2B43"/><TextBlock Text="内容保持在原来的位置，修改后共享内容也会更新。" FontSize="12" Foreground="#708099" Margin="0,5,0,0"/></StackPanel>
            </RadioButton>
            <RadioButton x:Name="CopyOption" GroupName="PublishMode" Style="{StaticResource Choice}">
                <StackPanel><TextBlock Text="保存一份副本" FontSize="14" FontWeight="SemiBold" Foreground="#1B2B43"/><TextBlock Text="复制到软件管理目录，原文件移走后副本仍可共享。" FontSize="12" Foreground="#708099" Margin="0,5,0,0"/></StackPanel>
            </RadioButton>
        </StackPanel>
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,8,0,0">
            <Button Content="取消" IsCancel="True" Style="{StaticResource DialogButton}" Background="#EDF2F8" Foreground="#1B2B43" Margin="0,0,8,0"/>
            <Button Content="确认发布" IsDefault="True" Click="Confirm_Click" Style="{StaticResource DialogButton}"/>
        </StackPanel>
    </Grid>
</Window>
