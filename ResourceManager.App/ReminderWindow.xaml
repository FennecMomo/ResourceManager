<Window x:Class="ResourceManager.App.ReminderWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="资源提醒" Width="480" SizeToContent="Height" ResizeMode="NoResize"
        WindowStartupLocation="Manual" Topmost="True" ShowInTaskbar="False" ShowActivated="False"
        Background="#F6F8FB" FontFamily="Segoe UI">
    <Window.Resources>
        <SolidColorBrush x:Key="Ink" Color="#1B2B43"/>
        <SolidColorBrush x:Key="Muted" Color="#708099"/>
        <SolidColorBrush x:Key="Accent" Color="#2867D9"/>
        <SolidColorBrush x:Key="Line" Color="#E4EAF2"/>
        <Style x:Key="BaseButton" TargetType="Button">
            <Setter Property="Height" Value="38"/>
            <Setter Property="Padding" Value="16,0"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="{StaticResource Accent}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="ButtonBorder" Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="9" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="ButtonBorder" Property="Opacity" Value="0.88"/></Trigger>
                            <Trigger Property="IsPressed" Value="True"><Setter TargetName="ButtonBorder" Property="Opacity" Value="0.74"/></Trigger>
                            <Trigger Property="IsEnabled" Value="False"><Setter TargetName="ButtonBorder" Property="Opacity" Value="0.42"/></Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="Button" BasedOn="{StaticResource BaseButton}"/>
        <Style x:Key="SecondaryButton" TargetType="Button" BasedOn="{StaticResource BaseButton}">
            <Setter Property="Background" Value="#EDF2F8"/><Setter Property="Foreground" Value="{StaticResource Ink}"/>
        </Style>
        <Style x:Key="QuietButton" TargetType="Button" BasedOn="{StaticResource BaseButton}">
            <Setter Property="Background" Value="Transparent"/><Setter Property="Foreground" Value="{StaticResource Accent}"/>
        </Style>
    </Window.Resources>
    <StackPanel Margin="24">
        <TextBlock Text="收到资源提醒" FontSize="20" FontWeight="SemiBold" Foreground="{StaticResource Ink}"/>
        <TextBlock x:Name="SenderText" Foreground="{StaticResource Muted}" FontSize="13" Margin="0,6,0,0" TextWrapping="Wrap"/>
        <Border Background="White" BorderBrush="{StaticResource Line}" BorderThickness="1" CornerRadius="14" Padding="18" Margin="0,18,0,0">
            <StackPanel>
                <TextBlock x:Name="ResourceNameText" FontSize="16" FontWeight="SemiBold" Foreground="{StaticResource Ink}" TextWrapping="Wrap"/>
                <TextBlock x:Name="ResourceMetaText" Foreground="{StaticResource Muted}" FontSize="12" Margin="0,6,0,0"/>
                <TextBlock x:Name="NoteText" Foreground="{StaticResource Ink}" FontSize="13" Margin="0,10,0,0" TextWrapping="Wrap"/>
            </StackPanel>
        </Border>
        <TextBlock x:Name="SentText" Foreground="{StaticResource Muted}" FontSize="12" Margin="0,10,0,0"/>
        <StackPanel Orientation="Horizontal" Margin="0,18,0,0">
            <Button x:Name="DownloadButton" Content="下载资源" Click="Download_Click" Margin="0,0,8,0"/>
            <Button x:Name="MuteButton" Content="静音此设备 1 小时" Click="Mute_Click" Style="{StaticResource SecondaryButton}" Margin="0,0,8,0"/>
            <Button Content="关闭" Click="Close_Click" Style="{StaticResource QuietButton}"/>
        </StackPanel>
    </StackPanel>
</Window>
